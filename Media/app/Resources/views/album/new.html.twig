{% extends 'base.html.twig' %}

{% block body %}
    <h1>Création d'album</h1>
    <div class="new-form">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Créer" />
        {{ form_end(form) }}
    </div>


    <ul>
        <li>
            <a href="{{ path('_index') }}">Retour à la liste</a>
        </li>
    </ul>
{% endblock %}
